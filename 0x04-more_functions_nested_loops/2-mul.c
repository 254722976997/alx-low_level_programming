#include "main.h"
#include <stdio.h>

/**
 * main -Entry point
 * @a: is integer 1.
 * @b: is integer 2.
 *
 *Return: Returns 0 always
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
